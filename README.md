# automatic_indices_pygmt_visualization
Project serves as a simple tool that allows to easily add different teledetection indices into Json, and display them using automatic cartographic module made with Pygmt.
